# NEXUS-7 桌面端前端环境变量（Vite，需 VITE_ 前缀）
# 复制为 apps/desktop/.env.local 后按需填写

# 后端 API 地址（默认 http://127.0.0.1:3737）
# VITE_NEXUS_API_BASE=http://127.0.0.1:3737

# Live2D 模型地址（.model3.json）。留空 → 使用内置 CSS 小人。
# 填入后前端会从 CDN 动态加载 Cubism 运行时并渲染真模型。
# 模型与 Cubism Core 受 Live2D 许可协议约束，不随仓库分发，请自备并遵守授权。
# 示例（自托管到 public/live2d/ 后）：
# VITE_LIVE2D_MODEL=/live2d/your-model/your-model.model3.json
# VITE_LIVE2D_MODEL=
