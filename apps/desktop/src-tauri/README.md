# Tauri Shell

Phase 1 uses the Vue desktop surface first. The Rust/Tauri runtime will be initialized after
Rust is available on the development machine.

Expected command once Rust is installed:

```powershell
corepack pnpm --filter @nexus/desktop tauri init
```
